<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Management System</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        fieldset {
            border: 1px solid #ccc;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
        }

        legend {
            font-size: 1.2em;
            color: #333;
        }

        form {
            display: grid;
            gap: 15px;
            margin-bottom: 20px;
        }

        input[type="text"], input[type="number"], input[type="date"], input[type="file"] {
            padding: 10px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1em;
        }

        button {
            padding: 10px 20px;
            border: none;
            background-color: #007bff;
            color: #fff;
            border-radius: 4px;
            font-size: 1em;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #f1f1f1;
        }

        td img {
            border-radius: 5px;
        }

        td a button {
            margin: 5px;
            padding: 5px 10px;
            font-size: 0.9em;
        }

        td a button:first-child {
            background-color: #28a745;
            color: white;
        }

        td a button:first-child:hover {
            background-color: #218838;
        }

        td a button:last-child {
            background-color: #dc3545;
            color: white;
        }

        td a button:last-child:hover {
            background-color: #c82333;
        }
    </style>
</head>

<body>

    <h1>Book Management System using CRUD</h1>

    <fieldset>
        <legend>Server Status</legend>

        <form action="/addData" method="post" >

            <input type="text" placeholder="Enter Image URL" name="book"><br>
            <input type="text" placeholder="Enter Book Title" name="name" required><br>
            <input type="number" placeholder="Enter Book Price" name="price" required><br>
            <input type="date" placeholder="Enter Book Release Date" name="date" required><br>
            <input type="text" placeholder="Enter Book Author Name" name="author" required><br>
            <input type="number" placeholder="Enter Book Author Sold Copies" name="copy" required><br>

            <button type="submit">Submit</button>
        </form>

        <table>
            <thead>
                <tr>
                    <th>Book Image</th>
                    <th>Book Name</th>
                    <th>Book Price</th>
                    <th>Release Date</th>
                    <th>Author Name</th>
                    <th>Sold Copies</th>
                    <th colspan="2">Actions</th>
                </tr>
            </thead>

            <tbody>
                <% students.map((e, i) => { %>
                    <tr>
                        <td><img src="<%= e.book %>" alt="Book Image" width="100" height="100"></td>
                        <td><%= e.name %></td>
                        <td><%= e.price %></td>
                        <td><%= e.date %></td>
                        <td><%= e.author %></td>
                        <td><%= e.copy %></td>
                        
                        <td>
                            <a href="/editData?id=<%=e.id%>">
                                <button>Edit</button>
                            </a>
                        </td>
                        <td>
                            <a href="/deleteData?id=<%=e.id%>">
                                <button>Delete</button>
                            </a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

    </fieldset>

</body>

</html>
